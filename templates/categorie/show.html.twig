{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <div class="offset-lg-3 col-lg-6">
    <h1 class="text-center mb-5">Categorie</h1>
    <table class="table table-striped table-hover">
        <tbody>
            <tr>
                <th>Identifiant</th>
                <td>{{ categorie.catId }}</td>
            </tr>
            <tr>
                <th>Catégorie</th>
                <td>{{ categorie.catNom }}</td>
            </tr>
        </tbody>
    </table>

    <div class="container">
        <div class="row">
            <div class="col-md mt-3">
                <a class="btn btn-secondary" href="{{ path('categorie_index') }}">Retour</a></div>
            <div class="col-md mt-3">
                <a class="btn btn-success" href="{{ path('categorie_edit', {'id': categorie.catId}) }}">Modifier</a>
            </div>
            <div class="col-md mt-3">
                {{ include('categorie/_delete_form.html.twig') }}
            </div>
        </div>
    </div>



</div>

{% endblock %}
