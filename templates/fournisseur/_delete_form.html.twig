<form method="post" action="{{ path('fournisseur_delete', {'id': fournisseur.fourId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fournisseur.fourId) }}">
    <button class="btn">Delete</button>
</form>
