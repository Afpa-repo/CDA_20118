{% extends 'base.html.twig' %}

{% block title %}Fournisseur index{% endblock %}

{% block body %}
    <h1>Fournisseur index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>FourId</th>
                <th>FourNom</th>
                <th>FourAdresse</th>
                <th>FourVille</th>
                <th>FourCp</th>
                <th>FourTel</th>
                <th>FourEmail</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for fournisseur in fournisseurs %}
            <tr>
                <td>{{ fournisseur.fourId }}</td>
                <td>{{ fournisseur.fourNom }}</td>
                <td>{{ fournisseur.fourAdresse }}</td>
                <td>{{ fournisseur.fourVille }}</td>
                <td>{{ fournisseur.fourCp }}</td>
                <td>{{ fournisseur.fourTel }}</td>
                <td>{{ fournisseur.fourEmail }}</td>
                <td>
                    <a href="{{ path('fournisseur_show', {'id': fournisseur.fourId}) }}">show</a>
                    <a href="{{ path('fournisseur_edit', {'id': fournisseur.fourId}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="8">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('fournisseur_new') }}">Create new</a>
{% endblock %}
