<form method="post" action="{{ path('facture_delete', {'id': facture.factId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ facture.factId) }}">
    <button class="btn">Delete</button>
</form>
